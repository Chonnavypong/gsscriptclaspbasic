{
  "name": "gswebapp",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "bld": "npx parcel build client/index.html --no-minify --no-source-maps --no-cache && node util.js && clasp push",
    "watch" : "nodemon --watch client --watch server --exec \"npm run bld\" --ext js, html, scss,css"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/google-apps-script": "^1.0.37"
  },
  "devDependencies": {
    "parcel-plugin-inliner": "^1.0.16",
    "sass": "^1.41.1"
  }
}
